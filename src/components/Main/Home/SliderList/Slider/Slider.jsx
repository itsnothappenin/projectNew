import React from 'react'

const Slider = (props) => {
    const slider = props.slider
    return (
        <div>
            {
              <h1>{slider._id}</h1>
            }
        </div>
    )
}

export default Slider